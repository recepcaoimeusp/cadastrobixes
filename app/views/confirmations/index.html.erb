<div class="col-md-6">
  <h1>Bixos não confirmados</h1>

  <div class="grid">
    <% @non_confirmed.each_with_index do |b, i| %>
      <div class="row list-item-<%= i % 2 %>">
        <h4>
          <%= link_to b.nome, "/bixos/#{b.id}/confirmations", method: :post %>
        </h4>
      </div>
    <% end %>
  </div>
</div>
<div class="col-md-6">
  <h1>Bixos confirmados</h1>

  <div class="grid">
    <% @confirmed.each_with_index do |b, i| %>
      <div class="row team-<%= b.team_color %>">
        <h4>
          <%= link_to b.nome, bixo_path(b.id) %>
        </h4>
      </div>
    <% end %>
  </div>
</div>
